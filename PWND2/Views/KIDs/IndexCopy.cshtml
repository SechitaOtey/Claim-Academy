@model IEnumerable<PWND2.Models.KID>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KidName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PointsGoal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KidName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PointsGoal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KidiD }) |
            @Html.ActionLink("Details", "Details", new { id=item.KidiD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KidiD })
        </td>
    </tr>
}

</table>
